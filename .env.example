# Copie este arquivo para .env e preencha com suas credenciais

# Flask Secret Key (gere uma chave forte)
SECRET_KEY=change-this-to-a-strong-random-key

# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

# Strava API Credentials (obtenha em https://www.strava.com/settings/api)
STRAVA_CLIENT_ID=your-strava-client-id
STRAVA_CLIENT_SECRET=your-strava-client-secret

# Redirect URI (ajuste para seu domínio em produção)
STRAVA_REDIRECT_URI=http://localhost:5000/strava/callback

# APIs de Tempo (opcionais - sistema usa Open-Meteo gratuita como fallback)
# WeatherAPI.com - 1000 chamadas gratuitas/dia (recomendado)
WEATHER_API_KEY=your-weather-api-key

# OpenWeatherMap - Alternativa (opcional)
OPENWEATHER_API_KEY=your-openweather-api-key

# OpenWeatherMap API Key (obtenha gratuitamente em https://openweathermap.org/api)
OPENWEATHER_API_KEY=your-openweather-api-key